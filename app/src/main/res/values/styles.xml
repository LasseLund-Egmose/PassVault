<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/DTU</item>
        <item name="colorPrimaryDark">@color/DTU</item>
        <item name="colorAccent">@color/DTU</item>
    </style>

    <style name="SwitchTheme">
        <item name="colorControlActivated">@color/DTU</item>
        <item name="colorControlNormal">#939393</item>
    </style>

    <style name="Theme.AppCompat.Translucent">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

</resources>